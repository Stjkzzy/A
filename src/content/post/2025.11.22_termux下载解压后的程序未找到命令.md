---
title: termux下载解压后的程序未找到命令
description: 记忆空间
publishDate: 2025-11-22
ogImage: /social-card.avif
---
在 Termux 中下载压缩包后解压得到的可执行文件出现 “command not found” 的情况，通常是以下几个原因导致的。下面按步骤说明检查与处理方法，帮助你把程序正常运行起来。

**1. 确认解压工具已安装**
如果系统里没有 `unzip`、`tar` 等解压工具，解压过程本身就会报错，导致文件未真正生成。可以先执行：

安装完成后再重新解压。
（参考：未安装 unzip 时会提示 “未找到命令”，解决办法是先安装 unzip）

**2. 为解压得到的文件添加可执行权限**
Linux（包括 Termux）默认会把新解压的文件权限设为普通文件，缺少执行位会导致 `command not found`。使用 `chmod` 为文件加上执行权限，例如：

如果是 Python 脚本，也需要同样处理（参考：对 sdat2img.py 进行 `chmod +x` 后即可运行）。

**3. 使用相对或绝对路径运行**
在当前目录下直接输入文件名会被当作系统命令搜索路径（PATH）中的可执行文件。如果文件不在*PATH*）中的可执行文件。如果文件不在PATH 中，需要显式指定路径：

**4. 检查文件是否对应设备架构**
Termux 运行在 Android 上，常见的 CPU 架构有 arm64‑v8a、armeabi‑v7a、x86、x86_64。若下载的二进制文件是针对其他架构编译的，系统会提示 “cannot execute binary file”。可以用 `file` 命令查看：

确认输出的架构与手机相符。如果不匹配，需要重新下载对应架构的版本，或使用兼容层（如 `proot-distro`）运行。

**5. 将常用目录加入 $PATH（可选）**‍
如果你希望以后直接通过文件名调用，可以把存放可执行文件的目录加入环境变量。例如把 `-/bin` 加入 PATH：

这样以后就可以直接敲 `myprogram` 运行。

**6. 确认 Termux 已获取存储权限**
如果文件放在外部存储（如 `/sdcard/Download`），需要先执行 `termux-setup-storage` 授权访问，否则会出现找不到文件的错误。

**7. 常见错误提示与对应处理**

**总结**：先确保解压工具已装、文件拥有执行权限、使用正确路径运行，并检查二进制是否匹配设备架构。完成上述步骤后，通常可以顺利执行在 Termux 中下载解压得到的程序。祝你使用愉快！

错误信息可能原因处理办法`command not found`未加执行权限或未使用 `./`路径`chmod +x 文件 && ./文件cannot execute binary file`二进制不匹配设备架构检查 `file`输出，下载对应架构版本`permission denied`没有读取/执行外部存储的权限`termux-setup-storage` 并确认文件所在目录可访问
